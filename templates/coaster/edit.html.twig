{% extends 'base.html.twig' %}

{% block title %}Modifier {{ coasterForm.vars.data.name }}{% endblock %}

{% block body %}
<div class="mb-4">
    <a href="{{ path('app_coaster_index') }}" class="btn btn-sm btn-ghost">
        ← Retour à la liste
    </a>
</div>

<div class="card bg-base-100 shadow-xl max-w-2xl mx-auto">
    <div class="card-body">
        <h2 class="card-title text-2xl mb-4">Modifier le coaster</h2>
        
        {{ include('coaster/_form.html.twig', {'button_label': 'Modifier'}) }}
    </div>
</div>
{% endblock %}
