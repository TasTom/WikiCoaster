{% extends 'base.html.twig' %}

{% block title %}Liste des Catégories{% endblock %}

{% block body %}
<div class="flex justify-between items-center mb-6">
    <h1 class="text-3xl font-bold">📁 Catégories</h1>
    {% if is_granted('ROLE_USER') %}
        <a href="{{ path('app_category_new') }}" class="btn btn-primary">
            Ajouter une catégorie
        </a>
    {% endif %}
</div>

<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
    {% for category in categories %}
        <div class="card bg-base-100 shadow-xl border-l-4" style="border-left-color: {{ category.color }};">
            <div class="card-body">
                <h2 class="card-title">
                    <span class="badge badge-lg" style="background-color: {{ category.color }}; color: white;">
                        {{ category.name }}
                    </span>
                </h2>
                <p class="text-sm text-base-content/70">Couleur: {{ category.color }}</p>
                
                <div class="card-actions justify-end mt-4">
                    <a href="{{ path('app_category_show', {id: category.id}) }}" class="btn btn-sm btn-info">
                        Voir
                    </a>
                    {% if is_granted('ROLE_USER') %}
                        <a href="{{ path('app_category_edit', {id: category.id}) }}" class="btn btn-sm btn-warning">
                            Modifier
                        </a>
                        <form method="post" action="{{ path('app_category_delete', {id: category.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette catégorie ?');" class="inline">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
                            <button type="submit" class="btn btn-sm btn-error">Supprimer</button>
                        </form>
                    {% endif %}
                </div>
            </div>
        </div>
    {% else %}
        <div class="col-span-full text-center text-gray-500 py-8">
            Aucune catégorie trouvée
        </div>
    {% endfor %}
</div>
{% endblock %}
